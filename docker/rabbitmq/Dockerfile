ARG RABBIT_VERSION
FROM rabbitmq:${RABBIT_VERSION}

RUN rabbitmq-plugins enable --offline rabbitmq_management

ARG RABBITMQ_PORT_1
ENV RABBITMQ_PORT_1 ${RABBITMQ_PORT_1}
ARG RABBITMQ_PORT_2
ENV RABBITMQ_PORT_2 ${RABBITMQ_PORT_2}
EXPOSE ${RABBITMQ_PORT_1} ${RABBITMQ_PORT_2}
